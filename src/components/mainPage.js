import React from 'react';

const ChooseOption = () => (
  <div className="container">
    <div />
    <input type="text" className="input--section" placeholder="Type / for blocks, @ to link docs or people" />
  </div>
);
export default ChooseOption;
